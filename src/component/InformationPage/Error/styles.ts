import { BoxProps, TextProps } from "@chakra-ui/react";

export const containerStyles: BoxProps = {
  textAlign: "center",
  p: 8,
};

export const titleStyles: TextProps = {
  fontSize: "xl",
  fontWeight: "bold",
  mb: 4,
};

export const messageStyles: TextProps = {
  mb: 4,
};
